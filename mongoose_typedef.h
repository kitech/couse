#ifndef MONGOOSE_TYPEDEF_H
#define MONGOOSE_TYPEDEF_H

#include "mongoose.h"

typedef struct mbuf mbuf;
typedef struct mg_connection mg_connection;
typedef struct mg_mgr mg_mgr;
typedef struct http_message http_message;
typedef struct websocket_message websocket_message;
typedef struct mg_http_multipart_part mg_http_multipart_part;
typedef struct mg_str mg_str;

#endif /* MONGOOSE_TYPEDEF_H */
